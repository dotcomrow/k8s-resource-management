# VPA

These manifests are vendored from:

  https://github.com/kubernetes/autoscaler/tree/master/vertical-pod-autoscaler/deploy

Update by copying the files referenced in `kustomization.yaml` from that path.
