# Namespaces

Goldilocks only reports for namespaces labeled:

  goldilocks.fairwinds.com/enabled: "true"

Add Namespace manifests here to set that label (or patch existing namespaces in your
workload repos if you do not want this repo to manage them).
